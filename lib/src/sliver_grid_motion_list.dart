import 'package:flutter/cupertino.dart';

class SliverGridMotionList extends StatelessWidget {
  const SliverGridMotionList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
